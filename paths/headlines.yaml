get:
  parameters:
    - name: limit
      in: query
      type: integer
  description: >
  
    [GET] /headlines
  
  response:
    '200':
      summary: Array<Headline>
      type: array
      items:
        - type: object
          $ref: '#/components/schemas/Headline'
