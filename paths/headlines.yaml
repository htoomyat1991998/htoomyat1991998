get:
  parameters:
    - name: limit
      in: query
      operationId: list-headlines
      summary: number of headlines to retrieve
      style: 
        type: integer
  responses:
    '200':
      description: Array<Headline>
      type: array
      items:
        $ref: '#/components/schemas/Headline'
