get:
  summary: number of headlines to retrieve
  parameters:
    - name: limit
      in: query
  operationId: list-headlines
  responses:
    '200':
      description: "Success"
      content:
        schema:
          type: array
          items:
            $ref: '#/components/schemas/Headline'
