get:
  parameters:
    - name: limit
      in: query
      type: integer
  description: >
    
    [GET] /articles  
  
  responses:
    '200':
      summary: Array<Article>
      type: array
      items:
        - type: object
          $ref: '#/components/schemas/Article'
      
