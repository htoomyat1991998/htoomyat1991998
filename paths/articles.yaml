get:
  summary: number of articles to retrieve
  parameters:
    - name: limit
      in: query
      schema:
        type: integer
        default: 10
  operationId: list-articles
  responses:
    '200':
      description: "Success"
      content:
        json:
          $ref: '#/components/schemas/Article'
